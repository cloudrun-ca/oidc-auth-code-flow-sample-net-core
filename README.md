# oidc-auth-code-flow-sample-net-core
OIDC Authorization Code Flow with offline_access scope (no PKCE)

set req'd values in config file [appsettings.Development.json](src/appsettings.Development.json) prior to running the app
